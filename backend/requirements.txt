fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
python-dotenv>=1.0.1
google-genai>=0.8.0
httpx>=0.27.2
Pillow>=10.4.0
tenacity>=9.0.0

# Durable run history + image storage. Optional at runtime: with no
# SUPABASE_URL configured the pipeline still writes results.json and the
# frontend still renders, so the demo never depends on it being reachable.
supabase>=2.10.0

# Sanity checks on the geo maths only. Not a test suite.
pytest>=8.3.0
